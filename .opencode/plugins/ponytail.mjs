/**
 * ponytail plugin
 * Auto-discovered from .opencode/plugins/ — explicit entry in opencode.json is optional.
 * @type {import("@opencode-ai/plugin").Plugin}
 */
export default async ({ client, project, directory, $ }) => {
  return {
    // config: (cfg) => {
    //   // mutate merged config
    // },
    // "tool.execute.before": async (input, output) => {
    // },
  }
}
